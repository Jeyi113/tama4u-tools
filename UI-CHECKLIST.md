# tama4u editor — UI 점검 체크리스트

> 1차 개선 반영됨(2026-08-12). `[x]`는 이번에 처리한 항목.

현재 구현 기준(2026-08-12). 한 항목씩 보면서 `[ ]` → `[x]` 표시하고,
문제가 있으면 그 줄 아래에 한 줄로 적어두면 그대로 수정 작업 목록이 됩니다.

실행: `python3 -m tama4u edit` → http://127.0.0.1:8477

---

## 0. 전체 레이아웃

- [x] **2열 그리드**(왼쪽 340px 정보 / 오른쪽 스프라이트)가 창을 좁혔을 때 어떻게 되는지 — 지금은 무너져서 오른쪽이 화면 밖으로 나감
- [x] `max-width:1200px` 고정이 큰 모니터에서 적절한지
- [ ] 카드가 세로로 길어질 때(호불호 60칸 + 좌표 14행) 스크롤 동선이 괜찮은지
- [ ] 스프라이트/시뮬레이션이 화면 아래로 밀려 처음에 안 보이는 문제
- [ ] 다크모드 대응 (현재 라이트 전용)

## 1. 헤더

- [ ] 파일명이 길면 잘리는지 / 줄바꿈되는지
- [ ] 기종 배지(`4U` `iDL` …) 위치와 눈에 띄는 정도
- [ ] 썸네일 미리보기(34px)가 너무 작지 않은지
- [ ] **저장 버튼이 항상 보이는지** — 스크롤해도 따라오는지(헤더는 sticky)
- [ ] 편집 후 저장 안 한 상태 표시가 없음 (변경사항 있음 표시 필요?)

## 2. 파일 열기

- [x] 드래그&드롭 영역 안내문이 "4U 아이템"으로 고정 — 이제 4기종 지원
- [ ] 잘못된 파일을 넣었을 때 안내가 토스트뿐 (드롭 영역에 남기는 편이 나은지)
- [ ] 폴더에서 여러 개 열어보며 비교하는 흐름이 없음 (`/api/list` 활용 가능)

## 3. 패킷 목록

> 패킷별 **.jpg 교체** 지원(중첩 포함, 부모 크기·체크섬 자동 보정).

- [ ] 중첩 표시(`↳` + 들여쓰기)가 한눈에 들어오는지
- [ ] 부가정보(`스프라이트 11 · 대사 22`)가 유용한지, 너무 빽빽한지
- [ ] 패킷이 1개면 카드가 숨겨짐 — 오히려 파일 구조를 못 보게 하는지
- [ ] 선택된 항목 강조(검은 배경)가 과한지

## 4. 기본 정보 카드

> 캐릭터 패킷에는 **캐릭터 스탯 카드**가 따로 뜬다(기상/취침·체중·감소율·
> 성격·스킬·생일·변신아이템 등 22개 필드 + 스킬 5칸).

- [ ] 이름 글자수 제한이 라벨에만 있음 — 초과 입력 시 즉시 피드백 없음
- [ ] 시리얼/가격/만복도/친밀도가 한 줄에 4개 — 좁을 때 찌그러짐
- [x] 행선지 드롭다운에 없는 코드면 잠기는데, 그 이유가 안내문으로만 표시됨
- [ ] 보관함 분류 힌트(시리얼 대역)가 길어서 카드를 밀어냄
- [ ] 애니메이션 ID가 숫자 입력만 — 어떤 동작인지 알 수 없음
- [ ] 값 범위 검증이 없음 (가격 65535 초과 등)

## 5. 조건부 표시(비활성화 규칙)

- [ ] 식사: 만복도·친밀도·호불호·스탯 표시
- [ ] 장난감: 만복도 숨김, 애니메이션 표시(4U)
- [ ] 옷·액세서리: 만복도·친밀도 숨김
- [ ] 방: 호불호까지 숨김
- [ ] 편지: 본문만, 나머지 전부 숨김
- [ ] 캐릭터 옷장: 초록 안내문 + 상점 필드 숨김
- [x] **숨기는 대신 비활성(회색)으로 두는 게 나은지** — 지금은 사라져서 레이아웃이 출렁임

## 6. 호불호 그리드

- [x] 4U는 캐릭터 이름, 나머지는 번호 — iD는 11칸으로 줄이고 안내문에 해당 버전 11명 명단 표시(확정된 2칸은 이름). iD L·P's는 여전히 32번호
- [x] 4열 고정이라 60칸이면 15줄 — 스크롤 부담
- [ ] 3-state 순환(없음→좋아함→싫어함)이 직관적인지, 되돌리기가 번거로운지
- [x] 색만으로 구분(초록/빨강) — 색약 대응 없음
- [ ] 전체 해제 버튼이 없음

## 7. 스프라이트 편집기

- [ ] 프레임 탭이 많을 때(28프레임, 17뱅크) 줄바꿈이 심함
- [ ] 뱅크 구분 라벨(`뱅크@0xa12`)이 사용자에게 의미 있는지
- [ ] 확대 배율 자동 계산 — 수동 줌이 없음
- [x] 팔레트 더블클릭으로 색 변경 — 발견하기 어려움
- [ ] 투명색(T) 표시가 팔레트 0번에만 — 규칙 설명이 힌트 텍스트뿐
- [ ] 실행취소가 Ctrl+Z만, 버튼 없음 / 다시실행 없음
- [ ] 그리기 도구가 점찍기 하나뿐 (선·채우기·사각형 없음)
- [ ] 픽셀 좌표 표시가 없음
- [x] BMP 가져오기/내보내기 버튼이 UI에 없음 (CLI에만 있음)

## 8. 썸네일

- [ ] "이 프레임으로 썸네일 만들기" 버튼 위치(탭 아래)가 맞는지
- [ ] 적용 후 헤더 미리보기만 바뀜 — 되돌리기 없음
- [ ] 배경이 흰색 고정 — 선택지가 없음

## 9. 애니메이션 미리보기

> 애니 재생은 **캐릭터·식사·간식**에만 표시. 옷·액세서리는 대신
> **체형 합성 나열**(8포즈 컷)이 뜬다. 장난감은 실제 순서가 ROM 동작 ID를
> 따르므로 프레임 순환을 보여주지 않는다.

- [x] 재생/정지 + fps 슬라이더로 충분한지
- [x] "뱅크 프레임 순환 재생"과 "실제 순서는 기기 동작 ID" 구분이 전달되는지
- [ ] 프레임 정렬이 바닥 기준 — 시뮬레이터(중심 기준)와 다름, 혼란스러운지

## 10. 기기 시뮬레이션

- [x] Body type 셀렉터가 옷/액세서리에만 나타남 — 다른 카테고리엔 시뮬 없음
- [ ] 더미 세트일 때 안내 문구가 라벨 끝에 붙어 잘 안 보임
- [ ] 재생 속도 고정(450ms) — 조절 없음
- [ ] 무대 배경이 단색 — 실제 방 배경을 깔 수 있는지
- [ ] 포즈 8종 고정 — 특정 포즈만 보기가 안 됨

## 11. 액세서리 좌표 편집

> 미리보기에서 액세서리를 **드래그**하면 1px 그리드에 스냅되어 좌표가 바뀌고
> 표·합성 나열이 즉시 갱신된다. 시뮬 그리드(4px 격자 + 중심선) 토글 추가.
> Body type 셀렉터를 바꾸면 좌표표·시뮬·합성 나열이 모두 그 체형으로 갱신된다.

- [ ] 14행 × X/Y 그리드가 세로로 길어 미리보기와 같이 못 봄
- [ ] 선택한 body type만 편집 — 4체형을 한 화면에서 비교 못 함
- [x] 미리보기에서 **드래그로 위치 조정**이 안 됨 (숫자 입력만)
- [ ] 다른 체형으로 값 복사 기능 없음
- [ ] 프레임 번호(0~14, 11 없음)의 의미 설명이 없음

## 12. 편지 본문

- [x] 런이 여러 개로 쪼개져 나옴 — 한 문장이 여러 칸에 걸침
- [x] 각 칸의 최대 글자수가 라벨에만 표시
- [ ] 초과 입력 시 저장 시점에야 에러 — 입력 중 경고 없음
- [ ] 전각 자동 변환이 일어나는 걸 사용자가 모름

## 13. 저장

- [ ] 저장 = 즉시 다운로드 — 미리보기/확인 단계 없음
- [ ] 파일명이 `<이름>_custom.jpg` 자동 — 변경 불가
- [ ] 성공 토스트만 뜨고 무엇이 바뀌었는지 요약이 없음
- [ ] 실패 시 원인이 영어 메시지 그대로 노출

## 14. 전반

- [ ] 한국어/일본어 혼용 — 용어 통일 필요한지
- [ ] 키보드 접근성 (탭 이동, 포커스 표시)
- [ ] 도움말/단축키 안내가 힌트 텍스트에 흩어져 있음
- [ ] 에러 상황(파싱 실패, 서버 끊김) 처리
