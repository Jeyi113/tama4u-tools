# 작업 체크리스트

순서대로 진행한다. 각 항목은 끝낸 뒤 근거(검증 수치)를 함께 적는다.

---

## 1. 카테고리별 구성·체크섬·규칙 정리 ✅
- [x] 카테고리 22종을 기종별(iD / iD L / P's / 4U)로 분류해 표로 정리
  - 악세사리, 옷, 외출지, 게임, 해피메일, 편지, 식사, 간식, 생활용품,
    장난감, 씨앗, 거실배경, 사진관 배경, 사진관 코스튬, 통신게임-레시피,
    스탬프 카드, 빙고게임, 캐릭터, 쿠폰, 데코레이션, 가샤폰
- [x] 각 카테고리의 destination 코드 · 유효 필드 · 스프라이트 규칙 · 크기 규칙
- [x] 체크섬/크기 필드 규칙 정리 (0x32 파일 크기, 0x4A 패킷 크기, 말미 sum16)
- [x] 팩 전수 조사로 빠뜨린 카테고리·기종 조합이 없는지 확인
- [x] `CATEGORIES.md`로 문서화

## 2. 카테고리별 스탯을 에디터에 전부 노출 ✅
- [x] 1의 표를 `items.py`의 유효 필드 정의에 반영
- [x] 캐릭터: 이름·성별 등 캐릭터 파일이 가진 항목 전체 편집 가능하게
- [x] 카테고리마다 빠진 필드 없는지 대조
- [x] JS 번들 동시 반영 + 패리티 확인

## 3. 중첩 패킷 .jpg 교체 ✅
- [x] 중첩 패킷을 다른 .jpg로 교체하는 기능 (이미 있었음 — 검증으로 확인)
- [x] 크기가 달라져도 상위 패킷 구조가 깨지지 않도록 (0x4A · 0x32 · 체크섬 갱신)
- [x] 왕복 검증 → `tests/test_nesting.py` (교체 90건)

## 4. 카테고리별 기본 구조 만들기 (신규 아이템 프레임) ✅
- [x] "만들기" 버튼 → 기종·카테고리 선택 → 해당 카테고리 프레임 생성
- [x] 카테고리 규칙 안에서 용량 최적화 (프레임마다 슬롯을 실제 크기에 맞춤)
- [x] 선택적 구성요소 처리 (액세서리 좌표 블록 iD 784 / 그 외 960)
- [x] 검증 → `tests/test_create.py` (재현 4,507건 중 91.7% 바이트 일치)

## 5. 모드 선택 UI를 토글로 ✅
- [x] 성별(남/여)처럼 값이 정해진 항목은 숫자 입력 대신 토글/선택으로
- [x] 해당하는 필드 전수 조사 후 일괄 교체
- [x] 첫 화면의 "새 아이템 만들기" 패널을 버튼 누르기 전까지 감춤

---

## 진행 기록

### 1번 (완료)
`CATEGORIES.md` 작성. 팩 4개 + VDP 20개, **패킷 6,360개 전수 조사**.

메운 공백 4가지:
- iD `0203xx02` = **사진관 배경** (13개). `0203xx01` 코스튬과 별개.
  파일명도 갈린다 — 배경은 `STUDIO__`(밑줄 2개), 코스튬은 `STUDIO_`.
- iD L / P's `81010101` · `81010102` = **비매품 식사·간식**.
  대부분 씨앗·해피메일·레시피·스탬프카드 **안에 중첩**돼 있어 안 보였다.
- iD `0101xx02` = 간식 변종 2개(도넛). 가격이 붙어 있어 비매품이 아니다.

밝혀낸 것:
- **쿠폰·데코레이션·가샤폰·프로모션은 전용 destination이 없다.**
  장난감 선반(`81042A01`)을 그대로 쓰거나(쿠폰·데코·프로모션),
  여러 카테고리를 섞은 묶음이다(가샤폰). 선반이 아니라 배포 형태다.
- 같은 코드가 기종마다 다른 선반인 경우 5건 (`81082900` 씨앗↔빙고 등).
- 스프라이트 개수는 카테고리마다 사실상 고정 (식사·간식 3, 옷 28,
  악세사리 4, 방 1). 4번 프레임 생성의 근거가 된다.
- 5스탯은 **P's·4U에만** 있다.

미분류로 남긴 것: iD `CDC0` 시그니처 5개. `models.py`에 이미 희귀 변종으로
적혀 있고 스탯이 성립하지 않아(가격 28,160) 분류에서 제외했다.
→ 6,360개 중 미분류 5개.

### 2번 (완료)
- **4U 캐릭터**: 손댈 것이 없었다. `character.py`가 아는 오프셋 25개가
  이미 전부 떠 있다(입력 28개) — ID · Revert ID · 그래픽 인덱스 · 스테이지 ·
  체형 · 성격 · 체중 · 감소율 · 병 확률 · 기상/취침 · 생일 · 옷/액세서리 분리 ·
  호불호 인덱스 · 변신 아이템 타입/시리얼/이름 · 스킬 5 · 두 번째 이름 ·
  대사 14줄 · 액세서리 좌표. 성별도 이미 선택형.
- **VDP 캐릭터**: 이름 · 성별 · 생일 · 변신 아이템 · 대사 6줄을 열었고,
  캐릭터를 고르면 그 캐릭터만 보이도록 했다. 미해독 (u16,u16) 쌍 둘도
  값만 열어 두었다.
  **캐릭터별 '좋아하는 아이템' 항목은 파일에 없다** — 접두부 4KB와 캐릭터
  패킷 14,664B를 전부 훑어도 그 VDP의 내용물을 가리키는 값은 변신 아이템
  하나뿐이고, 아이템 쪽 호불호는 기본 P's 32캐릭터용이다.
- **일반 아이템**: 카테고리 39조합에 대해 자리마다 값이 변하는지 세어
  대조했다. 메운 공백은 하나 — iD L 생활용품의 애니메이션. 일부러 닫아 둔
  자리와 그 근거는 `CATEGORIES.md` 6장에 적었다.

### 3번 (완료)
기능은 이미 있었다 — "이 패킷을 .jpg로 교체" 버튼이 `pk.path`를 쓰므로
중첩 경로에도 그대로 걸린다. 씨앗 파일의 1,202B 간식을 14,736B 씨앗으로
바꾸니 부모가 4,894 → 18,428B가 되고 형제 둘이 살아남았으며, 새 자식이
자기 중첩 셋을 데리고 들어왔다.

**회귀 테스트 신설** — `tests/test_nesting.py`. 중첩 카테고리 8종과 VDP
14개를 크기가 크게 다른 공여 아이템 셋(750B / 2,868B / 29,208B)으로
교체하며 체크섬·0x4A·0x32를 확인한다. 교체 90건 전부 통과.

**버그 하나를 잡았다** — 짝 파일 없이 잘린 VDP+를 편집하면 part 1이
32,768 → 26,340B로 줄면서 **part 2에 있던 내용물이 조용히 사라졌다.**
이제 거부한다.

조사하면서 밝혀진 것:
- **자식의 `0x32`는 그 아이템이 단독 파일이던 시절의 값**이다 (자식
  1,202B에 0x32가 2,310 = 그 파일의 jpeg + 패킷). 새 자리에서는 의미가
  없어 정품도 공여 값을 그대로 달고 있으므로, 교체 시 건드리지 않는 것이 맞다.
- **VDP 프로그램 패킷의 `0x32`는 실제 크기가 아니다** (vdp-009는 27,414B
  패킷에 704를 적어 둔다). 그래서 크기 변화를 그대로 반영할 수 없다.
- vdp-001이 압축 스트림 안(0x346, 스트림 시작 0x254)에 `TAMAGO`처럼 보이는
  **오탐 자식**을 하나 갖고 있고, 원본부터 체크섬이 깨져 있다.

### 4번 (완료)
`tama4u/create.py` 신설. 팩을 저장소에 넣을 수 없으니 템플릿을 복사하는
대신 **패킷을 직접 써 낸다.** 근거는 헤더가 이미 아는 필드로 다 설명된다는
것 — P's 식사와 4U 액세서리는 설명 안 되는 바이트가 **0개**, iD L 옷은 1개.

**검증 방식**: 정품 아이템을 그 아이템 자신의 값으로 다시 만들어 바이트를
비교한다(`create.reproduce`). **4,507건 중 4,132건(91.7%) 완전 일치,
길이 불일치 0건** — 길이가 맞는다는 건 레이아웃이 정확하다는 뜻이다.

남은 8%는 아직 해독 못 한 개별 바이트(iD 0x63·0xF0·0xFA, P's 0xA8,
4U 0xB2 등)로, 테스트가 목록을 출력한다. 새로 만든 아이템에서는 이 자리가
0이거나 `EXTRA_BYTES`가 주는 카테고리 상수다.

**⚠ 기기에서 시험해 보지 않았다.** 구조는 맞지만 미해독 바이트가 기기
동작에 필요한지는 확인되지 않았다.

**팔레트는 전 카테고리 16색으로 통일했다.** 처음엔 최빈값을 상한으로 적어서
P's·iD L 옷을 8색으로 막고 있었다. 16색이 픽셀당 4비트로 낼 수 있는 최대이고,
17색부터는 8비트라 용량이 두 배가 되므로 거기서 멈춘다. 팩의 실제 최대는
이보다 좁아서(4U 옷 8색, 액세서리 9색) 16색 4U 옷은 전례가 없다 — 코덱
안에는 있지만 기기에서 확인된 바 없다.

**장난감은 프레임 수를 고르게 했다.** 프레임 수가 곧 애니메이션이라 고정
청사진이 맞지 않는다. 팩 566개에서 1·2·3·4·6·7·8장이 나오고(5장은 없음),
프레임 수가 기하와 애니 값을 함께 정한다 — 3장짜리는 기종과 무관하게
32x30·48x32·40x20이다. 그래서 프레임 수를 고르면 애니 값이 따라 들어간다.

용량 최적화는 프레임마다 슬롯을 제 픽셀·팔레트 크기에 맞춰 잡는 방식이다.
8색 P's 옷 28장이 5,972B로 나오고(정품 6,976~15,472B), iD 사진관 배경은
4,140B로 정품과 정확히 같다.

만들 수 없는 것은 이유와 함께 거절한다 — 게임·외출지·VDP는 프로그램이고,
편지는 본문이 스탯 자리에 들어가며, 씨앗·레시피·빙고·스탬프카드·해피메일은
보상 아이템을 품는 구조라 기존 파일 편집이 안전하다.

### 5번 (완료)
전수 조사해 보니 **이름이 붙는 값은 이미 전부 선택형이었다** — 스테이지,
체형, 변신 아이템 타입, 호불호 인덱스는 `enum`, 옷/액세서리 분리는 `yesno`,
로스터 두 개는 전용 선택기. 숫자로 남은 것은 체중·감소율·병 확률·스킬처럼
눈금값이거나(4U 캐릭터 스탯), VDP의 미해독 쌍처럼 정체를 모르는 값이다.

**대신 빠져 있던 것을 찾았다 — 4U 캐릭터의 성별.** 0x21A를 읽어서
`char_stats.gender`로 보내고 `set_stats`도 쓰는데, 화면 목록(`CHAR_FIELDS`)에만
없어서 편집할 수 없었다. 선택형으로 추가했고 남자/여자로 표시한다. VDP
캐릭터 카드의 "남/여"도 같은 말로 맞췄다.

**첫 화면 정리** — "새 아이템 만들기" 패널이 버튼을 누르기 전에도 자리를
차지하고 있었다. `hidden` 속성을 걸어 뒀지만 인라인 `display:inline-flex`가
그걸 덮어쓰고 있었던 것. `[hidden]{display:none!important}`을 전역으로 넣어
같은 실수가 다시 나지 않게 했다.

### 후속 — 재현 못 한 8%의 정체 (2026-08-29)
"왜 재현을 못 했나"를 파고들어 **91.7% → 94.4%**로 올렸다. 원인은 셋이었다.

1. **아는 필드인데 안 넘긴 것** — iD의 펌웨어 호환 두 워드(`0xF8`~`0xFB`).
   값이 `0x0DC0`(Lovely Melody) · `0x1DC0`(리비전)이라 바로 알아볼 수 있었다.
   `items.get_version`이 이미 읽고 있었다. → +1.1%
2. **미해독 블록** — `0xF0`~`0xF7`. 그대로 넘기게 했다. → +1.6%
   그 과정에서 **4U 쪽은 해독됐다: 배포 기간**(아래).
3. **일부러 안 여는 자리** — iD 사진관 의상의 만복·친밀 오프셋.

**새 해독: 4U 배포 기간 `0xF4`~`0xF7`** = (시작 월, 일, 종료 월, 일). 7개
전부 성립하고 이름과 맞으며, 결정적으로 한 파일의 이름이
`sports-day-0502-0506.jpg`인데 바이트가 `05 02 05 06`이다. 에디터에 편집
가능한 필드로 노출했다.

**iD는 아니었다.** 같은 자리를 112개가 쓰지만 `0xF4`가 20·22·32·33처럼
월이 될 수 없는 값이라, 앞서 "iD도 같은 형태"라고 본 것은 틀렸다.

남은 5.6%: iD `0x63`(61개, 값 1 또는 2), P's `0xA8`(65개, 0~5),
iD 장난감 `0x68`, iD 액세서리 `0xF0` — 전부 정체 미상.

### 후속 — iD 계열·성별 구분 (2026-08-29)
**사진관 의상**은 오리지널 iD와 Lovely Melody가 호환되지 않는다.
에디터에 계열과 성별을 띄웠다. (액세서리도 함께 묶었다가 아니라고 해서
경고를 뺐다 — 계열 표시만 남긴다.)

- **계열**: `0x4C` 시그니처. `CD80` 오리지널(의상 33 · 액세서리 35),
  `0DC0` Lovely Melody(의상 6 · 액세서리 29). LMv 의상 6개는 **전부 여자용**.
- **의상 성별 = 프레임 수** — 남자 5장(6.1~6.2KB), 여자 6장(7.3~7.4KB).
  늘어난 한 장이 긴 머리다. 팩 39개가 파일명과 **39/39 일치**하고, 이름에
  성별이 없는 HEROIN·MCDONNA·OTOGIBANASHI도 전부 6장으로 갈린다.
- 액세서리는 두 계열의 구조가 같고(7장·784B) **성별 구분도 호환 문제도
  없다.** LMv만 `0xF0`/`0xF2`에 값이 있는데 **출시 차수 가설은 확인 결과
  틀렸다** — 1·2·3이 인접 인덱스에 번갈아 나오고 가격·종류·좌표·시리얼
  어느 축과도 맞지 않는다. 미해독으로 둔다.

---

## 6. 중첩 패킷을 갖는 카테고리도 만들기 (게임 → 외출지 → VDP)
- [x] 게임(`94025B01` / iD `1402xx00`+0x64=0x37) 만들기 — 기존 게임 바탕
- [x] 외출지 — 기존 외출지 바탕 + 구성(캐릭터·선물) 표시
- [ ] VDP 묶음

## 7. 파일 열기 UI 정리 ✅
- [x] 중앙 드래그 영역 제거, 우측 상단 "파일 열기" 버튼만 유지

## 8. "새 아이템 만들기" 버튼 위치 ✅
- [x] 파일 열기 버튼 옆으로 이동
- [x] 클릭 시 세부 설정을 박스(모달)로 표시

## 9. 호불호 노출 범위 바로잡기 ✅
- [x] 호불호는 식사·간식·악세사리·옷·장난감에만 있다 — 나머지에서 숨김
- [x] 호불호 아닌 카테고리에서 그 자리가 무엇인지 해석

### 6번 게임 (완료)
**게임은 중첩 패킷이 없었다** — 194개 중 0개. 실제 장벽은 S1C33 기계어라
코드를 합성할 수 없다는 것이다. `EZ_Gotchi`(268B)도 빈 껍데기가 아니라
`0xA6`·`0xAE`·`0xB0`과 두 기종에 공통인 꼬리(`9c c0 f4 6c 40 06`)를 가진
실제 내용물이 있는 패킷이었다.

그래서 **기존 게임을 바탕으로 만든다**(`create.from_base`). 코드는 그대로
두고 이름·시리얼·행선지만 새로 쓴다. 4U `touch-game`을 바탕으로 이름
MYGAME인 8,534B 게임을 만들어 확인했다.

**게임 분류를 찾았다 — 스프라이트 구성이 곧 엔진.** 중복 뺀 139개 중
99개가 다른 게임과 구성을 공유한다(76종 중 36종). `かいがらあわせ`와
`ジュエルハンター`는 이름도 크기도 다른 별개 게임인데 구성이 같다.
바이트 단위로는 공유가 없어(코드가 같은 9쌍은 전부 같은 게임의 중복 파일)
구성이 유일한 분류 축이다. 에디터에 "스프라이트 구성 (게임 엔진)"으로
띄운다 — 바탕 파일을 고를 때 이걸 보고 맞추면 된다.

### 7·8번 (완료)
- 중앙 드래그 영역(`#drop`)을 지웠다. 헤더의 "파일 열기"와 역할이 겹쳤다.
- "새 아이템 만들기"를 헤더로 옮겨 "파일 열기" 옆에 두고, 세부 설정은
  모달로 뺐다. 닫는 길 넷을 다 확인했다 — Esc · 배경 클릭 · 취소 · 만든 직후
  자동. 시트 안을 클릭했을 때는 닫히지 않는다.

### 9번 (완료)
호불호를 카테고리가 아니라 kind로 가리고 있었다. `items.LIKES_LABELS`로
바꿔 다섯 선반(과 이름만 다른 같은 선반)에만 연다.

**전부 0이던 곳 145개**: 씨앗 54 · 레시피 48 · 빙고 15 · 사진관 배경 13 ·
4U 방 12 · 생활용품 3. 한 비트도 세워진 적이 없다.

**iD 사진관 의상은 값이 있는데 호불호가 아니었다** — 39개 전부
`0x68`~`0x6B`에 값이 있지만 **1씩 증가하는 인덱스 표**다. 상위 니블이
남자는 1부터(`11 21 31 41`), 여자는 6부터(`61 71 81 91`) 시작하며,
프레임 수로 가른 성별과 39/39 일치한다. 캐릭터 포즈를 가리키는 표로 보인다.
호불호에서 빼면서 재현이 이 바이트를 잃길래 carry로 옮겼다(94.4% 유지).

### 후속 — 사진관 의상의 캐릭터, 외출지 구성 (2026-08-29)
**사진관 의상의 `0x68`~`0x6B` 상위 니블은 어느 캐릭터에 입히는지다.**
iD 로스터가 0~4 남자 · 5~9 여자인데, 남자 의상은 1~4를 여자 의상은 6~9를
든다. 스프라이트를 꺼내 보니 두 종류였다 — 상위 니블 0인 5개는 캐릭터가
옷을 입은 **완성 그림**이고(그래서 가리킬 캐릭터가 없다), 나머지 34개는
**옷 조각**이라 니블이 합성 대상을 가리킨다. 하위 니블 1·2·3은 여전히 미상.

**외출지**: 배경 128x72 한 장 + 캐릭터 스프라이트(대개 30x36, 캐릭터당 2장)
+ 대사 + 선물(중첩 패킷). 캐릭터 수는 장수의 절반이고 37개 중 4개가
홀수라 "약 N명"으로 적는다. 선물 수가 캐릭터 수와 같은 것은 10개뿐이고
15개는 선물이 없다 — 지역·협찬 외출지의 관습이지 일반 규칙이 아니다.
선물 아이콘이 섞이지 않도록 중첩 패킷 범위를 빼고 센다.

---

## 10. 프로그램(게임·외출지) 기종 이식
- [x] 호환 체크 vs 기종 변환의 차이 규명 · 문서화 (`CATEGORIES.md` 9장)
- [x] 이식에 필요한 변환 두 가지 특정 (펌웨어 호출 · `xld.w` 절대 주소)
- [x] 팩의 31쌍에서 지도를 배우는 도구 (`tools/learn_fwmap.py`)
- [x] 동기 문제는 없었다 — 패킷 밖 분기 2,529개가 31쌍에서 **전부** 짝이 맞는다
- [x] 2·4·6바이트 분기·`xld.w` 제자리 재인코더 (크기가 항상 일치함을 확인)
- [ ] 데이터에 박힌 주소 표 처리 — **여기서 막혔다**
- [ ] 이식기 구현 — **합격 기준: 31쌍이 바이트까지 재현** (현재 2/31)

### 10번 진행 상황
`0xFF` 호환 마스크는 한 바이트짜리 라벨이라 본문을 안 건드린다. 그래서
프로그램에도 쓸 수 있다. 기종 변환은 뱅크를 옮겨 재조립하는 일이라
프로그램에는 못 쓴다 — 스프라이트가 코드가 계산한 주소에 있다.

iD L ↔ P's만 라벨로 되는 이유는 **레이아웃이 호불호 위치와 5스탯 말고는
같기** 때문이다. 다중 표시 1,079개가 전부 iD L 파일이고, P's 호불호 칸은
비어 있다(=P's에서는 호불호 없는 아이템이 된다).

이식에 필요한 변환은 둘로 좁혀졌고 지도도 배웠다. **`xld.w` 주소 265종은
갈리는 것이 0개**로 완전히 일관된다. 적용하면 본문 차이가 202→2,
1,677→99바이트로 줄지만 0은 아니다. 막힌 곳은 지도가 아니라 디스어셈블러
동기다. 주소 하나가 틀린 게임은 실행되지 않으므로 그때까지는 내보내지 않는다.

### 10번 — 이식기가 막힌 지점 (2026-08-29)
앞서 "디스어셈블러 동기 문제"라고 본 것은 **틀렸다.** 패킷 밖 분기 2,529개가
31쌍에서 전부 짝이 맞고, 명령 크기도 (6,6)·(4,4)·(2,2)로 항상 같다.
그래서 제자리 재인코딩이 성립한다 — 원본 바이트를 그대로 되만드는 것까지
확인했다.

지도도 거의 완전하다: **분기 대상 407종 중 모호 1종, `xld.w` 주소 491종 중
모호 2종.**

두 지도를 적용하면 본문 차이가 202→2, 1,677→99바이트로 줄고 **31쌍 중
2쌍이 바이트까지 일치**한다. 남은 잔차의 정체는 **데이터에 박힌 주소 표**다:

```
iD:  ff 00 a4 0c   →   P's: 00 02 a4 0c      (0x00FF0CA4 → 0x02000CA4)
```

`xld.w`와 같은 창 이동인데, **어느 명령의 피연산자도 아니라서** 명령 단위
지도로는 닿지 않는다. 선형 디스어셈블이 본문 전체를 덮어 버려서 "데이터
구간"으로 따로 뽑아내지도 못했다(그 자리는 `.word`로 읽힌다).

다음 수: `.word`로 읽히는 자리에서 (상위 반워드, 하위 반워드) 쌍이
`0x00FF`/`0x00C0` 창에 드는 값을 표로 보고 옮기는 것. 다만 데이터에 우연히
그 패턴이 나오면 망가뜨리므로, 표의 시작·끝을 어떻게 가릴지가 관건이다.

**주소 하나가 틀린 게임은 실행되지 않으므로 31/31이 되기 전에는 내보내지
않는다.** 지금 상태는 `tools/learn_fwmap.py`로 지도를 뽑아 볼 수 있는 데까지다.

---

## 11. 시리얼 넘버 정리 ✅
- [x] 기종·카테고리별로 쓰인 시리얼 전수 조사 → `SERIALS.md`
- [x] 새 파일에 쓸 안전한 번호 특정 · `create.NEXT_SERIAL`로 내장
- [x] 만들기 모달이 그 번호를 미리 채우도록 연결

### 11번 (완료)
**가장 중요한 발견: 시리얼은 카테고리마다 따로 매겨진다.** 같은 기종
안에서도 선반이 다르면 같은 번호를 쓴다 — 4U 1001번을 거실배경·외출지·
액세서리·옷이 동시에 쓴다(공유 사례 iD 83 · iD L 357 · P's 282 · 4U 197).
그래서 새 번호는 **(기종, 카테고리)** 안에서만 겹치지 않으면 된다.

**시리얼 = ANSI 식별자의 카탈로그 번호**다. `t4u_gh01855_1`의 1855가 곧
시리얼이고 3,352개 중 3,322개(99.1%)가 일치한다.

**4U 캐릭터 슬롯(`tama_id`)은 별개**이고 두 블록으로 갈린다 —
`0x13xx`(5003~5042) 남자, `0x1Bxx`(7001~7037) 여자. 상위 바이트가 성별이다.

안전한 다음 번호를 카테고리마다 계산해 `create.NEXT_SERIAL`에 넣었고,
만들기 모달이 미리 채운다. 4U 장난감이면 1807, P's 식사면 16606이다.

**iD는 이것만으로 부족하다** — 행선지 셋째 바이트가 카탈로그 인덱스이고
기기가 검사하므로 "기기 버전" 카드에서 따로 지정해야 한다.

---

## 12. 게임 썸네일 합성 ✅
- [x] 정품 썸네일이 배경 + 스프라이트 합성임을 확인
- [x] 합성 위치가 파일에 없다는 것 규명 (레코드에 좌표 자리 없음)
- [x] 배경 위에 스프라이트를 놓는 합성 편집기

### 12번 (완료)
게임 썸네일은 **128x72로 배경 스프라이트와 같은 크기**이고, 배경 위에 다른
스프라이트를 올린 그림이다. `tea-time`의 배경만 꺼내 보면 울타리와 화단뿐이고
찻잔·캐릭터·숫자는 전부 얹은 것이다.

**합성 위치는 파일에 없다.** 스프라이트 레코드는
`[w][h][색수][00][프레임수][FF][팔레트][픽셀]`이 전부라 좌표 자리가 없고,
게임 중 위치는 코드가 실행하며 정한다. 액세서리의 착용 좌표 블록 같은 표가
게임에는 없다. 그러니 썸네일은 게임 화면의 재현이 아니라 작성자가 만든 표지다.

"썸네일 합성하기"를 넣었다 — 배경을 깔고 스프라이트를 골라 캔버스를 누르면
그 자리에 놓인다. 되돌리기·비우기·적용이 있고, 적용하면 스프라이트 원래
크기(128x72)로 JPEG을 만들어 미리보기에 넣는다. 배경은 0번 팔레트를 색으로
쓰고(불투명) 얹는 것은 0번을 비운다. 배경 스프라이트가 있는 패킷
(게임·외출지)에서만 버튼이 뜬다.

### 후속 — 배포 기간을 데코레이션에만 (2026-08-29)
배포 기간을 4U 전체에 열어 두었는데 **실제로는 데코레이션 6개에만** 있다.
나머지 1,018개는 그 자리가 0이다.

데코레이션은 전용 선반이 없어 장난감 코드를 쓰지만 **시리얼 대역이
갈라 준다** — 장난감 상점 `0x03xx`·`0x04xx`(144개), 쿠폰 `0x06xx`(5개),
데코레이션 `0x07xx`(6개). 기간 값이 있는 6개가 전부 `0x07xx`이고 나머지는
하나도 없다. 에디터에 이미 적혀 있던 보관함 분류 메모와도 맞는다.

`items.has_period()`로 그 대역에만 열도록 했다. 같은 장난감 선반이어도
1805(데코)는 뜨고 1109(장난감)·1601(쿠폰)은 숨는다. 4U 재현율이
95.7% → 97.4%로 올랐다 — 이제 안 건드리는 카테고리에서 그 바이트가 정확해졌다.
### 후속 — 캐릭터 편집기 정리 (2026-09-02)

**ANSI 번호와 시리얼은 별개 번호였다.** 캐릭터 파일은 패킷이 둘이다 —
카드(`_a2`, 행선지 `94024803`)와 0xB00에 든 몸(`_a1`, 행선지 `81033300`).
카드 시리얼은 1111~1183이고 ANSI 번호는 1601~1673인데, **몸 패킷의 시리얼이
ANSI 번호와 68/68 일치한다.**

그런데 `set_serial()`이 ANSI의 숫자를 무조건 따라 쓰고 있었다. 캐릭터 카드에서
시리얼을 저장하면 ANSI 1601이 1111로 덮여 몸과 어긋난다. 이제 **원래 둘이
같았을 때만** 동기화하고, 다를 때는 `set_ansi_num()`으로 따로 쓴다.

에디터에서는 ANSI 번호가 자기 칸을 갖고, 바꾸면 몸의 식별자와 시리얼이 함께
간다. 몸 패킷 이름도 같은 화면에 뒀다 — 새 캐릭터에 필요한 항목이 흩어져
있어서 패킷을 옮겨 다녀야 했다.

**캐릭터 카드를 그룹으로 나눴다.** 25개 필드가 한 덩어리로 붙어 있고 360px
사이드바에 갇혀 1,300px까지 늘어났다. 넓은 칸으로 옮기고 자리 / 프로필 /
생활 리듬 / 스킬 / 변신 / 호불호로 묶었으며, 거의 안 건드리는 넷은 접어 뒀다
(878x873). 선택지가 셋 이하인 항목은 드롭다운 대신 토글이다.

**성별 컨트롤이 둘이라 어긋나 보였다.** 슬롯 페이지(0x13 남 / 0x1B 여)와
0x21A 바이트가 따로 놀아서, 정품 여성 캐릭터가 슬롯은 여자인데 0x21A는 0이라
남자로 보였다. 기기가 실제로 보는 것은 슬롯 쪽이다 — 정품 여성 캐릭터가
전부 0x21A=0인데도 여성으로 인식된다.

그래서 **슬롯을 단일 기준으로 삼고 0x21A를 거기에 맞춘다.** 토글을 누를
때뿐 아니라 **파일을 열 때도** 맞춘다. 처음엔 토글에만 걸어 뒀는데, 그러면
열자마자 둘이 어긋난 채로 남아 있었다. 값이 바뀌는 경우에는 라벨에
`슬롯에 맞춰 0 → 1`이라고 적어 무엇이 달라지는지 숨기지 않는다.

정품과 달라지는 지점이다 — 정품 여성 캐릭터는 0x21A가 0인데 여기서 저장하면
1이 된다. 기기가 이 바이트를 보지 않는다는 근거(정품 33명이 전부 0인데도
여성으로 동작)로 무해하다고 판단했다.

**옷 분리 / 액세서리 분리를 맞바꿨다** (0x21C = 액세서리, 0x21E = 옷).
UJ가 기기 동작으로 짚어 줬고, **팩으로 확정됐다** — 0x21C는 착용 좌표표를
가진 캐릭터와 **68/68 일치**하고, 0x21E는 34/68로 우연 수준이다. 둘 다
0=No, 1=Yes. Mr.Blinky 에디터와의 단일 필드 diff로 잡았던 자리라 짝이
뒤집히기 쉬운 경우였다.

### 후속 — 0x21A는 성별이 아니다

정품 68개가 **전부 0인데 그중 33명을 기기가 여성으로 인식한다.** 성별은
`tama_id`의 로스터 페이지(0x13 남 / 0x1B 여)가 정한다. Mr.Blinky 에디터의
이름을 그대로 따 왔던 것이라 `byte_21a`로 바꾸고 성별 드롭다운에서 뺐다.

`graphics`(0x208)도 68개가 전부 0이다. **둘 다 값이 하나뿐이라 편집해서
배울 것이 없고 틀리면 파일만 위험하므로 읽기 전용으로 잠갔다** —
`character.READONLY`가 저장 경로에서 걸러낸다.

### 후속 — 성격 타입에 이름을 붙일 수 없다

0x20C는 정품에서 1~14가 쓰이고 7만 없다. 뜻은 파일에 없다 — 성별·체형·
스킬·호불호 어느 축과도 상관이 없다(스킬은 대부분 50으로 균일하다).

대신 **같은 값을 쓰는 정품 캐릭터를 라벨에 적었다.** 기기가 프로필에
성격을 보여 주므로, 목록에 있는 캐릭터 하나를 기기에서 확인하면 그 번호가
무엇인지 알 수 있다.

### 후속 — 착용 좌표표를 체형별로 연다

합성 창이 표를 `[0]`에서만 읽고 고칠 때는 **네 체형에 같은 값을 덮어썼다.**
표를 가진 17명 중 16명은 네 행이 실제로 같지만 **たまここひめ는 다르다** —
셋째 행 앞에 `00` 한 바이트가 끼어 뒤 두 행이 1바이트씩 밀려 있고, 그래서
(64,42)가 (42,64)로 읽힌다. 정렬 문제로 보이지만 단정하지 않았다.

이제 합성 창에서 체형을 고르고, "네 체형 함께"를 끄면 한 체형만 고칠 수
있다. 네 행이 서로 다르면 경고를 띄운다. 기본값은 종전대로 네 체형 함께다.

### 후속 — 3분할 VDP+ 지원 (2026-09-03)

`ciao-vdp-plus`처럼 **파일이 셋인 VDP+**를 열 수 없었다. 짝 파일을 하나만
받는 구조였다. 소장본 21개 중 **12개가 3분할**이라 절반 이상이 막혀 있었다.

파트 순서는 **아이템 이름의 마지막 글자**가 정한다(`CIAO…1`·`…2`·`…3`) —
21개 번들 전부 그렇고, 단독 피어스(`VDP★NNN★`) 50개는 숫자로 끝나지 않아
구분도 된다. 순서가 중요하다: Ciao를 1+2+3으로 읽으면 내용물 23개가 전부
체크섬 정상이고, 1+3+2로 읽으면 12개에서 멈춘다. 아무 순서로 넘겨도 되도록
받는 쪽에서 정렬한다.

`split_parts`를 N분할로 일반화했다 — 마지막을 뺀 모든 파트를 32,768B로
채우고 나머지를 다음으로 넘긴다. 21개 번들 전부 무편집 왕복이 바이트 동일.

**"나머지 파일 불러오기"가 안 뜨던 문제**: 잘림 판정에만 걸어 뒀는데,
`anniversary`는 part 1만으로 내용물이 **0개**라 잘림이 False로 나온다.
이제 파트 번호가 있으면(=VDP+ part 1이면) 항상 띄운다. 중간 파트가 빠진
경우(1+3)도 번호의 구멍으로 잡아 경고한다 — 잘림 판정은 이걸 놓친다.

**속도**: 서버 0.04~0.16초, JS 62ms로 두 구현 다 빠르다. 지연은 재현되지
않았고, 중복 파싱 한 곳(`_part_gap`이 파트너를 다시 파싱하던 것)만
없앴다. 합치는 동안 버튼이 "합치는 중…"으로 바뀐다.

### 후속 — anniversary 파트 불러오기 · 압축기 · easter 편집 버그 (2026-09-03)

**anniversary는 "나머지 파일 불러오기"가 안 떴다.** VDP 카드가 내용물이
있을 때만 열리는데(`vdpCard.hidden=!rows.length`), anniversary part 1은
혼자서 내용물이 0개라 카드째 숨어 그 안의 버튼도 안 보였다. 멀티파트면
내용물이 0개여도 카드와 버튼이 뜨도록 고쳤다.

**LZ 압축기를 개선했다.** anniversary는 페이로드가 세 파일 용량의 ~1KB
안쪽에 붙어 있어, 우리 압축기가 반다이 것보다 조금 커서 편집 저장이 용량
초과로 실패했다. 셋을 더해 되샀다 — 후보 탐색 깊이 64→1024, 한 칸 lazy
matching, 길이 2 매치 허용. anniversary 98,081→96,248B(용량 97,070 안),
ciao 87,148→85,397, easter 58,100→56,917, vdp-009 24,426→23,716. 전부
왕복 정확, Python·JS 바이트 동일.

**easter(2분할) 편집이 스트림을 깨뜨리던 버그.** 압축 스트림이 우연히
`TAMAGO`를 담으면 컨테이너가 그걸 중첩 자식으로 잘못 읽는데(easter part 1에
하나 있음, ciao·anniversary엔 없음), 저장 시 `apply_edits`가 `pkt.raw`만
갈아끼우고 옛 Packet 객체를 그대로 둬서, `build_file`의 체크섬 패스가 그
가짜 자식을 새 스트림의 엉뚱한 위치에 덮어썼다. 이제 파트 바이트에서
Packet을 새로 파싱하고 가짜 자식을 비운다. `split_parts`/`write_subs`도
끝 2바이트에 진짜 체크섬을 봉인해, 바이트를 다시 파싱하는 호출부에서도
유효한 파일이 나오게 했다. easter·ciao·anniversary 편집 저장 21/23/7개
콘텐츠 유지, 체크섬 전부 통과, Python·JS 동일.

### 후속 — anniversary 변신 스프라이트를 추출한다 (2026-09-04)

anniversary는 변신 캐릭터 스프라이트가 있는데도 안 잡혔다. 원인: **구형
체인지 피어스는 변신 몸통을 TAMAGO 헤더 없이 저장한다.** ciao·babychange
등 신형은 몸통이 옷 선반 콘텐츠 패킷(헤더 있음)이라 콘텐츠 워커가 읽지만,
anniversary(2016)는 육성 조건 블록과 첫 콘텐츠 사이에 **빈 0x100 헤더 +
28장 포즈 뱅크**를 매직 없이 stride 0x39BA로 열 개 늘어놓는다. TAMAGO
스캔이 통째로 건너뛰었다.

`vdp.bare_bodies()`가 이 영역을 찾아, 각 뱅크 앞에 합성 헤더를 붙인 패킷을
돌려준다 — 다른 캐릭터 몸통처럼 읽고 편집된다. 육성 조건 블록 10명과
1:1(PRINCE MAME…WEDDING HIME)이라 이름도 붙는다. 신형 번들(옷 선반 콘텐츠에
캐릭터 몸통이 이미 있는 것)에서는 자동으로 비활성이라 중복되지 않는다.

편집기 목록에 "· 변신 몸통"으로 뜨고, 클릭하면 28장 포즈가 로드되며
(pose_bank라 체형 합성은 숨김), 스프라이트를 고쳐 저장하면 페이로드의 그
자리(헤더 뒤 뱅크만)에 다시 써넣는다 — 빈 헤더는 건드리지 않아 기기 포맷이
유지된다. `vdpbody` 경로로 처리하고, 저장은 압축을 다시 하는 경로를 탄다.
anniversary 10개 몸통 추출·스프라이트 편집·저장·왕복 확인, Python·JS 동일.

### 후속 — ◎를 해로, 스프라이트 선택·이동·복사 (2026-09-04)

**붙여넣기 후 이동에서 원본이 지워지고 잔상이 남던 버그.** 이동을 "잘라서
다시 찍기"로 구현해서, 붙여넣은 픽셀을 옮기면 아래 가려졌던 원본이 구멍(0)으로
드러나고, 각 드래그 단계가 이전 위치에서 시작해 잔상이 쌓였다.

떠 있는 레이어(`flt`) 방식으로 다시 썼다 — 이동/붙여넣기는 픽셀을 base 위에
뜬 층으로 들어 올리고, 드래그 매 단계 **깨끗한 base에서 다시 합성**한다.
- **이동**: base = 선택을 지운 프레임, 층 = 들어 올린 색. 지나간 자리는
  base가 그대로라 잔상이 없고, 원위치는 투명해진다.
- **붙여넣기**: base = **손대지 않은 프레임**이라, 층을 끌어 옮겨도 아래
  원본이 보존된다. 커밋 전까지 굽지 않는다.
투명 픽셀은 안 찍어 배경이 비쳐 보인다. 확정은 도구 변경·다른 클릭·Esc·
복사·프레임 전환에서. 옷 프레임으로 붙여넣기 후 원본 위를 지나 이동 →
가려진 원본 보존·경로 잔상 없음, 일반 이동 도착 일치·원위치 비움 확인.

**0x62는 해다.** ▽와 같은 사연 — 대조 때 가장 가까운 글자가 `◎`였을 뿐.
`GLYPH_FIX`에 `◎`→`☀`를 더하고 `◎`·`☼`를 별칭으로 받는다. `◯`(0x5F)은
별개 글자라 손대지 않았다. 정품 `ホワイトデー♥`·`そりっちルーム☀`로 확인.

**스프라이트 편집기에 선택·이동·복사를 넣었다.** 도구 3개(그리기/선택/이동):
- **선택** — 사각형 드래그. 여러 번 드래그하면 **합집합**으로 쌓인다
  (16px + 12px = 28px 확인). 선택 영역은 반투명 칠 + 점선 윤곽.
- **이동** — 선택 픽셀을 드래그로 옮긴다. 원래 자리는 0번(투명)으로 비고,
  선택도 함께 따라간다.
- **복사/잘라내기/붙여넣기** (Ctrl+C/X/V) — 복사는 인덱스가 아니라 **색(RGB)**을
  저장해 팔레트가 다른 프레임에 붙여도 색이 유지된다(가장 가까운 인덱스로
  매핑, 자리 있으면 추가). 붙여넣으면 이동 도구로 전환돼 바로 옮길 수 있다.
프레임을 바꾸거나 새 파일을 열면 선택은 지워진다. Esc로 선택 해제.

### 후속 — ▽를 하트로, 썸네일 합성 편집 (2026-09-04)

**0x61은 하트다.** 문자표가 `▽`로 적어 둔 건 다운로드 이름을 대조할 때 그
코드에 가장 가까운 글자가 그것뿐이었기 때문이다. 기기 폰트는 하트로 그린다.
`GLYPH_FIX`로 표시를 `♥`로 바꾸고, `GLYPH_ALIAS`로 `♥`·`▽`·`♡`·`❤`를 모두
같은 코드로 입력받는다. Python·JS 동일.

**썸네일 합성 창을 편집기로 키웠다.** 전에는 눌러 놓으면 고정이었다. 이제:
- 올린 요소를 **드래그**로 옮긴다 (잡은 지점 기준이라 튀지 않는다).
- 눌러 고른 뒤 **회전(±90°) · 좌우/상하 뒤집기 · 삭제**. 변형은 새 프레임을
  만들어 적용해 그리기·히트테스트가 단순하다.
- **BMP 다운로드** — 합성 결과를 24비트 BMP로 저장(하단 정렬·BGR·패딩).
게임 tea-time으로 배경 128x72 위 스프라이트 배치·드래그(28,27→58,42)·90°
회전+좌우 뒤집기·BMP(BM 27,702B 128x72 24bpp) 확인.

### 후속 — H1 대사를 찾지 못했다 (2026-09-03)

변신장의 등장 캐릭터(H1)에 대사가 있어야 한다는 지적을 받고 Ciao 번들의
**모든 영역을 훑었다. 대사는 파일에 없다.**

| 영역 | 크기 | 내용 |
|---|---|---|
| 로더 (스트림 앞) | 812B | S1C33 기계어 |
| 페이로드 접두부 `0x00`~`0x20` | 32B | 외출장소명(`0x0A`) · 캐릭터 수(`0x1B`) |
| 캐릭터 블록 ×10 | 각 384B | 이름 · 성별 · 생일 · **대사 6줄** · 미해독 쌍 2개 |
| 블록 이름 뒤 | 각 184B | `0xFF` 패딩 + 끝에 u16 두 개(0x0060/0x00C9, 블록마다 +1) |
| 접두부 꼬리 `0xF20`~`0x1002` | 226B | 플래그 + `0xFF` 패딩 |
| 콘텐츠 23개 | 168,034B | 전부 스프라이트 뱅크 — H1은 1,734B가 뱅크로 꽉 찬다 |
| 콘텐츠 사이 빈 구간 | 캐릭터마다 1,922B | 전부 `0x00` |
| H1 헤더 `0x5E`~`0x100` | 162B | 이름과 작은 필드 몇 개, 나머지 0 |

**대사가 있는 곳은 캐릭터 블록 10개뿐이고, 그건 변신 캐릭터 10명 것이다**
(BIRTHDAY / 청혼 / 배고픔 / 간식 / 놀이 / 잡담). 블록 수는 변신 몸통 수와
정확히 같아서(10/10 · 7/7 · 3/3) H1용 블록이 따로 있지도 않다.

부수적으로: **H1~H4 세트는 변신형 번들에만 있다** — ciao · babychange ·
disneychange에는 있고, 신 캐릭터 추가형(easter · circus · caveman ·
berrysweets)에는 없다. 기기 쪽 동작이나 다른 파일에 있는 것일 수 있어
단정하지 않고 여기 적어 둔다.

`anniversary`는 3파트를 다 합쳐도 내용물이 7개뿐이다 — 별도 확인 필요.

### 후속 — 변신형 VDP는 캐릭터 크기가 다르다

UJ: 해독되던 VDP는 **신 캐릭터 추가**형이고, 안 되던 것은 **기존 캐릭터의
변신 상태**를 더하는 형이다. 후자는 변신 모습 · 변신장(외출장소) · 추가
아이템으로 구성되고 새 캐릭터는 없다.

`is_char_content`가 크기 14,664를 못 박고 있었다. 그 근거는 2분할 번들만
본 것이었고 — 3분할 12개는 열리지 않아 조사에서 빠졌다 — 변신형의 캐릭터
몸통은 **12,856B**라 옷으로 읽혔다. 그래서 28장 포즈가 옷 아이템 취급을
받아 체형 합성이 4세트×7조각으로 잘랐고, 그게 "스프라이트가 오독된다"의
정체였다.

이제 **스프라이트 뱅크로 가른다**: 몸통은 28장이 포즈(최대 46~62)이고,
드레스는 28장이 착용 조각(최대 24)이다. Easter가 둘 다 갖고 있어 대조가
된다 — 14,664B 몸통 셋(최대 52·가격 0)과 8,824B 변신 드레스(최대 24·
가격 1,200). Ciao의 변신 모습 10개가 접두부의 육성 조건 블록 10개와
이름까지 1:1로 맞는다.

### 후속 — 변신장(외출장소) 이름과 구성

**이름은 압축 페이로드 `0x0A`의 8글자**(1바이트 인코딩)다. CIAO · EASTER ·
DISNEY · CAVEMAN · CIRCUS · BABY · SWEETS — 푸는 번들마다 제 피어스 이름과
맞는다. 에디터에서 편집 가능하게 열었다. 페이로드에 있으므로 압축을 다시
하는 경로를 탄다.

구성은 이름이 H1~H4로 끝나는 네 콘텐츠다 (UJ):

| | 내용 | Ciao 실측 |
|---|---|---|
| H1 | 등장 캐릭터 | 1,734B · 30x46 ×2 + 4x4 |
| H2 | 외출장소 바깥 | 4,908B · 128x72 |
| H3 | 외출장소 내부 | 4,908B · 128x72 |
| H4 | 변신 애니메이션 | 1,220B · 30x20 ×3 |

각각 다른 상점 코드를 달고 있어 방·간식으로 보이던 것을 이름으로 갈라
라벨을 붙였다. **H1의 대사는 아직 찾지 못했다** — 콘텐츠 패킷 안에는
텍스트가 없고, 접두부의 대사는 변신 캐릭터 10명 것이다.

### 후속 — 캐릭터 합성에 내장 옷도 얹는다

옷장 뱅크는 캐릭터 자신의 옷도 **16~22번**에 들고 있는데 타일이 포즈와
액세서리만 그려서 벗은 모습이 나왔다. 이제 포즈 → 옷 → 액세서리 순으로
겹친다. 대응은 UJ가 준 값이다:

| 옷 | 포즈 |
|---|---|
| 16 | 1 · 2 · 4 |
| 17 | 3 · 5 · 6 · 11 |
| 18 | 7 · 8 · 13 |
| 19 | 9 |
| 20 | 10 |
| 21 | 14 |
| 22 | 15 |

옷 조각은 옷 *아이템*과 같은 규칙으로 놓는다 — 가로 가운데, 아래 맞춤.
액세서리와 달리 좌표표를 쓰지 않는다. "내장 옷" 체크로 끌 수 있다.

### 후속 — 합성 화면 정리

크기 입력 칸을 없앴다. BMP 가져오기가 크기를 스스로 맞추니 먼저 숫자를
넣을 일이 없다 (`resizableFrame()`은 그 판단에 계속 쓰인다).

**기기 시뮬레이션을 껐다.** 옷·액세서리 전용이었고 둘 다 포즈별 자기
화면이 생겨 겹치는 자리였다. 식사·간식의 애니메이션 미리보기는 그대로다
(시뮬레이션은 원래 식사·간식에서는 뜬 적이 없다).

**VDP의 옷 선반 콘텐츠는 체형 합성에서 뺀다.** 몸통이든 변신 드레스든 특정
캐릭터 전용이라, 범용 기준 캐릭터에 합성하면 아무 의미가 없다 (`char_shelf`).
합성은 어느 체형에도 맞는 상점 옷을 위한 것이다.

**체형 합성 스트립은 옷에만 남긴다.** 옷은 체형마다 7조각 세트가 따로라
입은 모습을 볼 곳이 여기뿐이다 — 체형1은 30x12, 체형3은 30x16으로 다른
세트를 쓴다. 액세서리는 같은 것을 착용 좌표 타일이 좌표까지 붙여 보여
주므로 뺐다. 체형 선택이 시뮬레이션 안에 있었으므로 스트립에도 자체
선택(`stripBody`)을 붙였다.

**착용 좌표 카드를 시뮬레이션 밖으로 뺐다.** 그 안에 들어 있어서 시뮬레이션을
끄면 함께 사라졌다. 이제 자기 체형 선택(`accBody`)을 갖고 독립해서 뜬다.

### 후속 — BMP 가져오기가 크기를 스스로 맞춘다

크기가 다른 BMP는 거부하고 있었다. 슬롯이 늘어날 수 있는 자리(캐릭터
23~26번)에서는 BMP가 이미 크기를 말해 주므로 먼저 숫자를 입력할 이유가
없다 — 그 프레임에서는 가져온 BMP 크기를 그대로 따른다. 나머지는 크기가
의미를 갖는 자리(기기가 포즈·조각을 크기로 고른다)라 종전대로 거부하되,
어디서 크기를 바꿀 수 있는지 메시지에 적었다.

28x22는 원래 슬롯에 들어가 파일 크기가 그대로였고, 40x36은 슬롯 736으로
늘며 몸 15,468 → 15,864B · 카드 18,398 → 18,794B가 됐다. 둘 다 체크섬
검증을 통과하고 나머지 27프레임은 그대로다.

### 후속 — 액세서리 아이템의 착용 좌표도 합성 타일로

액세서리 아이템을 열면 좌표표가 **숫자 상자 28개짜리 표**로만 떴다. 어느
포즈에 어디로 붙는지는 전혀 보이지 않고, 확인하려면 옆의 기기
시뮬레이션이 그 포즈까지 돌아오기를 기다려야 했다.

이제 캐릭터 내장 액세서리와 같은 방식으로 그린다 — **행마다 타일 하나**,
고른 체형의 기준 캐릭터 위에 그 행의 스프라이트를 저장된 중심 좌표대로
얹고, 끌어서 옮기거나 아래 X·Y로 고친다. 체형은 시뮬레이션의 선택을
따라가고, "액세서리만"으로 캐릭터를 끌 수 있다.

**빠져 있던 두 행을 찾았다.** `AC_PRIMARY`(포즈 → 그 포즈에 쓰는 스프라이트)에
포즈 1과 15가 없었다. 시뮬레이션은 8포즈만 돌아서 드러나지 않았지만,
포즈별 타일로 바꾸니 14행 중 12개만 나왔다 — 두 행이 편집할 수 없는 상태로
남아 있었던 것이다. 1은 다른 서 있는 포즈를, 15는 14를 따르게 했다
(캐릭터 쪽 대응표도 14·15를 같은 프레임 26으로 묶는다). 이제 14행이 전부
나온다.

iD(스프라이트 7장) · iD L · 4U(4장) 전부 4체형×14행으로 뜨고, 액세서리가
아닌 아이템에서는 카드가 뜨지 않는다. 한 행만 고쳐 저장하면 그 행만 바뀌고
다른 행·다른 체형은 그대로다.

### 후속 — 애니메이션은 식사·간식만

프레임 순환 미리보기를 캐릭터 포즈 뱅크에도 켜 뒀는데, 28프레임은 한
애니메이션이 아니라 서로 다른 포즈라 무관한 그림 사이를 깜빡일 뿐이다.
이제 `gh`(식사) · `oy`(간식)에서만 돈다. 옷·액세서리·장난감·방·캐릭터는
전부 꺼지고, 옷·액세서리는 종전대로 몸에 합성한 스트립을 보여 준다.

**캐릭터 스프라이트 23~26번이 커질 수 있게 했다.** 내장 액세서리 자리는
2x2로 나오는데 실제로 그릴 것을 넣으면 슬롯을 넘긴다. 슬롯이 고정이라
`frame data N exceeds slot M`으로 거부됐다.

이제 그 네 프레임에만 크기 입력이 뜨고, 키우면 슬롯이 늘어난다. 길어진
패킷이 무너뜨리는 값은 전부 따라간다 — 그 패킷의 0x4A, 감싼 패킷의 0x4A,
모든 최상위 0x32, 그리고 체크섬. `replace_packet`이 스플라이스한 바이트로
Packet을 다시 만들기 때문에 새 Packet이 자기를 "안 건드린 것"으로 보고 짧던
시절의 체크섬을 그대로 들고 있었다 — 스플라이스 직후에 봉인해서 고쳤다.
2x2 → 24x20에서 슬롯 42 → 278, 파일 +236B, Python·JS 결과가 바이트 단위로
같다. 포즈 프레임(1~22, 27~28)은 기기가 크기로 색인하므로 열지 않았다.

### 후속 — 4U 외출지 스프라이트도 크기 다른 BMP 가져오기

외출지(외출) 스프라이트는 count-bank가 아니라 프로그램 blob 꼬리에 헤더째
붙은 **loose 레코드**라, 지금까지는 원본과 같은 크기 BMP만 제자리 덧칠할 수
있었다. 이제 다른 크기의 BMP도 받아 레코드를 통째로 다시 만든다 —
`encode_loose(w,h,palette,pixel_lists)`로 `[w][h][ncol][00][nf][FF][pal][px]`
전체를 새로 짓고, 옛 레코드 자리에 스플라이스한 뒤 길어진 만큼 값들을
따라간다(0x4A, 최상위 0x32, 체크섬). 한 번의 저장에 loose 뱅크 17개가 함께
오므로, 키운 레코드 **뒤쪽 형제 뱅크들의 offset도 늘어난 만큼 밀어** 준다
(안 그러면 제자리 덧칠이 어긋난 주소를 친다). 30x36 → 40x40에서 파일 +260B,
Python·JS 결과가 바이트 단위로 같다(32694B). 다만 외출지는 프로그램이라
레코드가 밀리면 구조는 멀쩡해도(체크섬 통과) 기기 화면상의 위치가 어긋날 수
있어, 가져오기 전에 그 경고를 확인받는다.

### 후속 — 0번 인덱스를 투명색으로 지정할 때 무조건 초록이 되던 오류

BMP 가져오기가 팔레트 0번을 언제나 초록(투명 표식)으로 덮어썼다. 원래
불투명 프레임(배경·리빙)이 0번을 실제 색으로 쓰는데도 초록으로 바뀌어
버렸다. 이제 가져온 팔레트를 그대로 보존하고, 스와치도 0번이 투명일 때만
체커보드로 보이게 했다(초록 강제 안 함). 원본이 투명 프레임이었는데 가져온
0번이 초록이 아니면 "이 스프라이트는 불투명이 됩니다(투명하게 하려면 0번을
초록으로)"라고 알려 준다.

### 후속 — 외출지 loose 스프라이트의 팔레트 색 수 바꾸기

외출지 스프라이트를 편집할 때 팔레트 색을 지우면 `팔레트 크기를 고정시켜야
한다`며 저장이 막혔다. loose 레코드는 헤더에 색 수를 적어 두므로 색을 더하거나
빼면 레코드 길이가 달라지는데, 제자리 덧칠(`write_loose`)이 크기를 고정으로
강제했기 때문이다. 크기 변경 자체는 이미 grow 경로(`encode_loose`)가 레코드를
새로 짓고 뒤 레코드들의 offset을 밀어 처리하고 있었다.

이제 외출지 loose에서 팔레트 색을 더하거나(팔레트 `+` 버튼) 미사용 색을 지울 수
있고, 저장할 때 색 수가 원본과 다른 loose 뱅크는 자동으로 grow 경로를 탄다
(`loose[3]`=원본 ncol과 현재 팔레트 길이 비교). 게임 등 grow를 흡수할 수 없는
loose는 종전대로 색 수 고정. `+` 버튼은 4bpp 경계인 16색까지만 열어 8bpp로
넘어가지 않게 한다. 크기가 바뀌면 뒤 스프라이트가 밀려 실기 위치가 틀어질 수
있으므로, 파일당 한 번 확인을 받는다(BMP 리사이즈 확인과 공유).

30x36 4색에서 4→5(+2B)·4→3(-2B) 모두 checksum 통과, 외출지로 재인식,
Python·JS 결과 바이트 동일. 실제 서버(/api/build→/api/parse) 왕복도 확인.
덤으로, 같은 크기라도 색 수가 다른 BMP를 외출지에 가져오면 grow가 안 서서
저장이 실패하던 잠재 버그도 함께 고쳐졌다.

### 후속 — 외출지 스프라이트의 투명색(팔레트 0번) 보호

외출지를 편집하다 스프라이트의 팔레트 0번이 초록(투명 표식)에서 벗어나자
기기에서 게임이 망가졌다. 팩 전수 조사로 규칙이 확정됐다: 외출지의 비배경
스프라이트(128x72가 아닌 모든 레코드)는 100% 팔레트 0번이 투명 초록
(RGB 0,255,0 = BGR565 0x07E0, 일부 0x07E6)이고, 전체화면 128x72만 투명(21)/
불투명 배경(22)으로 갈린다. 기기는 레코드의 0번을 투명 키로 읽으므로 거기에
실색이 들어가면 스프라이트가 통째로 덩어리로 그려져 장면이 깨진다.

이전에 "0번 강제 초록"을 전역으로 없앤 것이 원인이었다 — 방 배경(불투명)엔
맞지만 외출지 스프라이트엔 틀렸다. 이제 각 외출지 레코드의 투명/불투명을
로드 시점에 고정(`trans0`)하고, `frOpaque`이 실시간 팔레트가 아니라 그 고정값을
따른다. 투명 레코드는 0번을 초록으로 지킨다: BMP 가져오기는 대상이 원래
투명이면 0번을 초록으로 되돌리고(불투명 배경은 가져온 실색 유지), 0번 색 변경·
순서 변경을 막고, 저장 직전 투명 레코드의 0번이 초록이 아니면 되돌린다(유효한
초록 변종 0x07E6은 보존). 삭제는 0번이 투명 픽셀에 쓰여 이미 막혀 있다.

검증: 투명 스프라이트에 빨강 0번 BMP 가져오기 → 0번 [0,255,0]로 유지, 불투명
배경에 파랑 0번 BMP → 실색 유지, 0번 강제로 빨강 후 저장 → 서버 결과 [0,255,0]·
외출지 재인식. 모두 브라우저 end-to-end.

### 정정 — 외출지 레코드는 크기 고정 (리사이즈/팔레트 색 수 변경 철회)

앞서 외출지 스프라이트의 크기 변경(grow)과 팔레트 색 수 변경을 허용했으나,
실기에서 스프라이트만 같은 크기로 교체해도 색 수가 다르면 깜빡임 + 스프라이트
미표시로 게임이 깨졌다. 조사 결과 외출지 레코드의 스트라이드가 고정이 아니라
각 레코드의 span(=6+2*ncol+w*h/2)과 정확히 일치했다 — 즉 프로그램이 런타임
순차 파싱이 아니라 **미리 계산해 박아둔 위치**로 레코드를 참조한다는 뜻이고,
따라서 레코드가 1바이트라도 늘거나 줄면(색 수는 색당 2바이트) 뒤 레코드가
전부 어긋난다.

그래서 외출지 레코드는 **바이트 크기를 절대 바꾸지 않는다**:
- 크기 다른 BMP 가져오기 거부(외출지는 resizableFrame에서 제외).
- 같은 크기 교체 시 팔레트를 원본 색 수(loose[3])로 패딩해 레코드 길이 유지,
  원본보다 색이 더 필요한 BMP는 거부("최대 N색").
- 팔레트 + 버튼·색 삭제를 외출지에서 비활성화(색 수 고정).
- 저장 시 grow 마킹 제거(투명 0번 초록 보장은 유지).

검증: 4색 스프라이트에 2색 BMP → 팔레트 4색 패딩·저장 크기 32434 불변,
마젠타 2색 BMP 교체 → 내용 바뀌되 크기 불변·외출지 재인식·index0 초록 유지,
6색 BMP·40x40 BMP → 각각 거부. 캐릭터 23~26 리사이즈(count-bank)는 그대로.

### 재개 — 외출지 스프라이트 리사이즈, 이번엔 제대로 (4바이트 정렬)

"레코드 변경됐다고 그렇게 고장나는 게 말이 안 된다"는 지적이 맞았다. 외출지
파일들을 대조해 기기의 스프라이트 접근 방식을 역산했다:

- 기기는 각 loose 레코드 헤더로 `recordlen = 6 + 2*ncol + 픽셀바이트`를 계산해
  **4의 배수로 올려** 다음 레코드로 순차 이동한다. 스프라이트는 **순서(index)로
  참조** — 저장된 오프셋 테이블이 없다. 헤더-워크 시뮬레이터가 리테일 31/31
  파일의 레코드를 정확히 재현했다.
- 스프라이트 뒤의 **대사 텍스트·디스패치 테이블·중첩 액세서리 패킷**도 절대
  오프셋으로 참조되지 않는다(경계값 전수 검색 0건, 중첩 있는 20파일 포함). 그래서
  레코드가 밀려도 전부 함께 밀릴 뿐 깨지지 않는다.

**진짜 버그는 4바이트 정렬 누락이었다.** 이전 `encode_loose`가 레코드를 4의
배수로 패딩하지 않아, 색 수가 +1(홀수 델타 +2)만 돼도 뒤 레코드 전부가 기기
워크에서 어긋났다(6개만 복원 → 깜빡임·미표시). scan_loose는 0xFF 시그니처로
찾으니 도구 검증은 통과했지만 기기는 stride로 걸으므로 깨졌다.

수정: `encode_loose`가 레코드를 4의 배수로 zero-pad, `loose_span`이 그 4-정렬
stride를 반환, 리사이즈는 정렬 길이가 같아도 제자리 splice(색만 바뀐 경우 in-place
writer가 색 수 고정을 요구하므로). Python·JS 동일. UI 재활성화: 외출지에서 크기
다른 BMP 가져오기·팔레트 +/삭제 모두 열되 파일당 한 번 확인. 투명 0번 초록 보장
유지.

검증: 4→5색은 **델타 0**(5번째 색이 정렬 패딩에 들어가 파일이 안 커짐), 4→6색
+4, 30x36→40x40 +260, 전부 외출지 재인식. 리테일 31/31 파일에서 중간 스프라이트를
40x40으로 리사이즈 후 기기-워크 전 레코드 복원 + 체크섬 + 중첩패킷 바이트 무결.
Python↔JS 바이트 동일. 실기 최종 확인은 사용자 몫으로 남김.
